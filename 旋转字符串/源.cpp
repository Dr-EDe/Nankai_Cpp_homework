#include<iostream>
using namespace std;


int main() {
	//前半和后半分别倒置，最后再倒置一次
	char str[100];
	cin >> str;
	int offset; cin >> offset;
	int i = strlen(str);
	for (int a = 0; a <= offset - 1; a++) {
		char b = str[offset-a-1];
		
	}









	system("pause");

	return 0;

}